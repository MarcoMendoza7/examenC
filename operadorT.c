#include <stdio.h>

int main (){
    int cliente;
    float cantidad;
    float descuento;
    float resultado;
    float operador;

    puts("Ingrese el numero 1 si es cliente. Numero 2 si no lo es");
    scanf("%d", &cliente);
    puts("Ingrese la cantidad en pesos");
    scanf("%d", &cantidad);
    operador = cliente ? descuento = ((cantidad * 0.20)(resultado = cantidad - descuento)) : 
    descuento = ((cantidad * 0.05)(resultado = cantidad - descuento))
    printf("Su resultado final es = %f\n", resultado); 
}